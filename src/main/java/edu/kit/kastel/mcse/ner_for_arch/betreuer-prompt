
response = client.chat(model='phi4', messages=[
  {
    'role': 'system',
    'content': 'You are a software engineer and software architect.',
  },
  {
    'role': 'user',
    'content': '''
    In the following text, which architecturally relevant entities like software components or interfaces are directly named?
    For each architectural entity, provide the lines numbers where the entity appears.
    Text:
    {text}
    '''.format(text=text_to_use),
  },
])
print(response["message"]["content"])
